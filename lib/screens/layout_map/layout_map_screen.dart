import 'package:flutter/material.dart';

class LayoutMapScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Layout maps'),
      ),
    );
  }
}
