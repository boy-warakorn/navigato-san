import 'package:flutter/material.dart';

class RoomScreen extends StatelessWidget {
  static const routeName = '/rooms';
  @override
  Widget build(BuildContext context) {
    return Center(child:Text('This is room pages'));
  }
}
